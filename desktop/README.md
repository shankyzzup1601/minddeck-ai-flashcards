# MindDeck AI for Windows

This Electron app loads the live MindDeck study experience in a dedicated Windows desktop window.

Run `npm install` and then `npm run dist:win` to generate a portable Windows executable.
